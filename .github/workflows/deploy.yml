name: Disabled - Pages automation removed

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op (Pages automation removed)
        run: echo "This workflow is intentionally disabled to avoid GitHub Pages automation."
