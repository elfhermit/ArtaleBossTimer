/*
  Root-level `app.js` archived: the active/runtime copies are in `/docs/app.js` and `/public/app.js`.
  This placeholder keeps the repository root clean while preserving the source in `src/` and the deployable site in `docs/`.
*/

console.info('Root app.js archived. Use /docs or /public for the runnable site.');
